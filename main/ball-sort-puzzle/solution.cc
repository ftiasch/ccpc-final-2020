#include <bits/stdc++.h>

int main() {
    while (scanf("%d%d", &n, &m) == 2) {
        std::vector<std::vector<int>> stacks;
        for (int i = 0; i < n; ++ i) {
            int size;
            scanf("%d", &size);
            for (int j = 0; j < size; 
        }
    }
}
