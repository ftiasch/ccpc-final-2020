#include <bits/stdc++.h>

#include "common.h"

namespace {
int a[N][N];
}

int main() {
  int n, m, h, w, k;
  while (scanf("%d%d%d%d%d", &n, &m, &h, &w, &k) == 5) {
    for (int i = 0; i < n; ++i) {
      for (int j = 0; j < m; ++j) {
        scanf("%d", a[i] + j);
      }
    }
    for (int i = 0; i + h <= n; ++i) {
    }
  }
}
